# Milestone M0 - Bootloader Development

## Branch Attuale
```
milestone/M0
```

## Commit History
```
af1901f [M0][Boot] Activate custom page tables before kernel handoff
c89146e [M0][Boot] Allocate UEFI page tables with AllocatePages
4ceb795 [M0][Boot] Remove UEFI console calls after ExitBootServices
e2459ce [M0] Baseline before milestone start
ceb7e34 pre-analysis snapshot
195a22d Document UEFI bootloader execution blocker and debugging steps
```

## Task Completati

### Task 1: UEFI Page Table Allocation
- **File modificati**: `uefi/boot.c`
- **Descrizione**: Implementazione dell'allocazione delle tabelle di pagina UEFI con `AllocatePages`

### Task 2: UEFI Console Calls Removal
- **File modificati**: `uefi/boot.c`
- **Descrizione**: Rimozione delle chiamate alla console UEFI dopo `ExitBootServices`

### Task 3: Custom Page Tables Activation
- **File modificati**: `uefi/boot.c`, `uefi/efi.h`, `Makefile`
- **Descrizione**: Attivazione delle tabelle di pagina personalizzate prima del passaggio al kernel
- **Fix ABI/struct**: Ripristinata la chiamata `EFIAPI` nella funzione `efi_main` e correzioni alle strutture UEFI

## Hotfix Session: Full UEFI Boot Chain Debug (branch debug/uefi-unsupported)

### Fix 1: Stale elf_load.o — Makefile Header Dependency
- **Sintomo**: crash `#GP` a 0xAFAFAFAF (poisoned memory), `BootServices` letto a offset errato
- **Causa**: `uefi/elf_load.o` compilato prima delle correzioni alle struct in `efi.h`, Makefile non aveva dipendenza sull'header
- **Fix**: aggiunta regola `uefi/%.o: uefi/%.c uefi/efi.h` in `Makefile`; rebuild forzato con `rm -f uefi/elf_load.o uefi/boot.o && make uefi`
- **Proof**: `objdump -d uefi/elf_load.o` mostra `mov 0x60(%rdi),%rax` (BootServices) e `call *0x140(%rax)` (LocateProtocol) ✓

### Fix 2: kernel.elf assente nell'immagine ESP
- **Sintomo**: `ELF load: 0x8000000000000004` = `EFI_NOT_FOUND`
- **Fix**: `mcopy -i dist/test_esp.img -o dist/kernel.elf ::kernel.elf`

### Fix 3: GetMemoryMap Buffer Too Small (seconda chiamata)
- **Sintomo**: `GetMemMap final: EFI_BUFFER_TOO_SMALL (0x8000000000000005)`
- **Causa**: `final_map_size = map_size` usava la dimensione aggiornata da UEFI dopo la prima chiamata (byte effettivi usati), non il buffer allocato; nuovi descrittori aggiunti da allocazioni intermedie non entravano
- **Fix**: aggiunta variabile `alloc_buf_size = map_size` prima della prima `GetMemoryMap`, usata per la seconda chiamata

### Fix 4: ExitBootServices EFI_INVALID_PARAMETER — Retry Loop
- **Sintomo**: `ExitBootServices: EFI_INVALID_PARAMETER (0x8000000000000002)`
- **Causa**: chiamate `puts16` tra `GetMemoryMap` e `ExitBootServices` facevano allocare memoria al firmware, invalidando il MapKey
- **Fix**: retry loop silenzioso — `GetMemoryMap` + `ExitBootServices` senza nessuna chiamata a console in mezzo; aggiunto `#define EFI_INVALID_PARAMETER` mancante in `efi.h`

### Fix 5: GOP Use-After-Free dopo ExitBootServices
- **Sintomo**: `#PF` a RIP spurio dopo "[BOOT] Calling ExitBootServices", RAX = `0x894D5741FA1E0FF3`
- **Causa**: `gop->Mode->Info` acceduto dopo `ExitBootServices` (puntatori a memoria UEFI freed)
- **Fix**: salvataggio di `FrameBufferBase`, `HorizontalResolution`, `VerticalResolution`, `PixelsPerScanLine` in variabili locali PRIMA di `GetMemoryMap`/`ExitBootServices`

### Fix 6: GOP Struct Layout Errate in efi.h (3 bug)
- **Sintomo**: `gop` validato (`0x80000000`), ma struct letta con offset sbagliati → puntatori garbage
- **Bug 1**: `EFI_GRAPHICS_OUTPUT_PROTOCOL` — mancava campo `Blt` (8 byte) a offset 16 → `Mode` letto a +16 invece di +24
- **Bug 2**: `EFI_GRAPHICS_OUTPUT_PROTOCOL_MODE` — campi in ordine errato; `FrameBufferBase` a offset 0 invece di 24 (spec UEFI: `MaxMode`, `Mode`, `*Info`, `SizeOfInfo`, `FrameBufferBase`, `FrameBufferSize`)
- **Bug 3**: `EFI_GRAPHICS_OUTPUT_MODE_INFORMATION` — mancava `PixelInformation[4]` (12 byte) → `PixelsPerScanLine` a offset 16 invece di 32
- **Fix**: riscrittura completa di tutte e tre le struct in `efi.h` conformi alla spec UEFI §12.9

### Fix 7: _start 32-bit incompatibile con Long Mode UEFI → _uefi_start
- **Sintomo**: `_uefi_start` non ancora definito; boot UEFI chiamava `e_entry` = `_start` (codice BITS 32)
- **Causa**: `_start` è codice Multiboot2 32-bit; chiamato da long mode UEFI → istruzione `A3` letta come 9-byte in 64-bit (misalignment); poi `jmp 0x08:long_mode` (opcode `0xEA`) = invalid in 64-bit → `#UD` → triple fault silenzioso (no IDT)
- **Fix**: aggiunto entry point `_uefi_start` BITS 64 in `boot/boot.asm` — ricarica GDT, segmenti, stack (`lea rsp, [rel stack_top]`), chiama `kernel_main` via SysV ABI
- **Fix complementare**: `uefi/elf_load.c` — aggiunto lookup simbolo ELF `_uefi_start` nella symbol table; se trovato, sovrascrive `e_entry` come indirizzo di entrata

### Verifica Finale
```
[BOOT] entered efi_main          ✓
GOP found: 0x0000000080000000    ✓
[OK] Mappa memoria acquisita     ✓
[OK] Kernel ELF caricato, entry= 0x000000000020019D  ✓  (_uefi_start)
[OK] Segmento copiato vaddr= 0x0000000000200000      ✓
[OK] Seconda GetMemoryMap acquisita                  ✓
[BOOT] Calling ExitBootServices                      ✓
debugcon: 'U'                                        ✓  (_uefi_start raggiunto)
kernel_main chiamata                                 ✓
```

## M0 – Final Stabilized State

### Lista definitiva hotfix applicati (in ordine cronologico)

| # | Fix | File | Sintomo risolto |
|---|-----|------|-----------------|
| 1 | Rimozione console calls post-EBS | `uefi/boot.c` | UB su firmware reale |
| 2 | AllocatePages per page tables | `uefi/boot.c` | Allineamento 4KB garantito |
| 3 | Attivazione page tables custom | `uefi/boot.c` | CR3 caricato con PML4 propria |
| 4 | Dipendenza header Makefile | `Makefile` | `elf_load.o` stale → `#GP` a 0xAFAFAFAF |
| 5 | kernel.elf nell'immagine ESP | `dist/test_esp.img` | `EFI_NOT_FOUND` |
| 6 | `alloc_buf_size` per seconda GetMemoryMap | `uefi/boot.c` | `EFI_BUFFER_TOO_SMALL` |
| 7 | ExitBootServices retry loop | `uefi/boot.c` + `uefi/efi.h` | `EFI_INVALID_PARAMETER` |
| 8 | GOP save-before-EBS | `uefi/boot.c` | use-after-free → `#PF` |
| 9 | GOP struct layout (3 bug) | `uefi/efi.h` | offset sbagliati → puntatori garbage |
| 10 | `_uefi_start` BITS 64 entry point | `boot/boot.asm` | triple fault silenzioso da `_start` 32-bit |
| 11 | ELF symbol lookup `_uefi_start` | `uefi/elf_load.c` | entry point sbagliato (`_start` invece di `_uefi_start`) |

### Stato del boot (M0 completato)

```
[BOOT] entered efi_main          ✓
GOP found: 0x0000000080000000    ✓
[OK] Mappa memoria acquisita     ✓
[OK] Kernel ELF caricato, entry= 0x000000000020019D  ✓  (_uefi_start)
[OK] Segmento copiato vaddr= 0x0000000000200000      ✓
[OK] Seconda GetMemoryMap acquisita                  ✓
[BOOT] Calling ExitBootServices                      ✓
debugcon: 'U'                                        ✓  (_uefi_start raggiunto)
kernel_main chiamata                                 ✓
```

### Architettura handoff UEFI → kernel

```
OVMF firmware
  └─► BdsDxe loads BOOTX64.EFI (PE32+, x86-64)
        └─► efi_main() [uefi/boot.c]
              ├─ GOP query → framebuffer base, size, pitch salvati
              ├─ GetMemoryMap (con alloc_buf_size)
              ├─ elf_load_kernel() → carica kernel.elf, trova _uefi_start in .symtab
              ├─ Costruisce page tables (PML4/PDPT/PDT, identity 512MB, 2MB pages)
              ├─ Attiva PAE + CR3 + LME + NXE + paging [già in long mode UEFI]
              ├─ ExitBootServices() con retry loop silenzioso
              ├─ Popola secos_boot_info {fb_addr, fb_w, fb_h, fb_pitch, mem_map, ...}
              └─► call _uefi_start [boot/boot.asm, BITS 64]
                    ├─ lgdt [rel gdt.pointer]  (GDT propria del kernel)
                    ├─ segmenti = 0x10 (data), SS = 0x10
                    ├─ lea rsp, [rel stack_top]
                    └─► call kernel_main(RDI=0, RSI=&secos_boot_info)
```

**Calling convention**: SysV AMD64 — `kernel_main(uint32_t magic, uint64_t info)`:
- `magic = 0` → path UEFI (distinto da `0x36d76289` Multiboot2)
- `info` → puntatore fisico a `secos_boot_info` (identità mappato, valido)

### Motivazione tecnica split entrypoint 32-bit / 64-bit

Il kernel ELF ha un unico `_start` (BITS 32) come entry point Multiboot2: è codice 32-bit
protetto che imposta page tables e fa `jmp 0x08:long_mode` (opcode `0xEA`).

Il path UEFI arriva già in **long mode 64-bit**. Chiamare `_start` da long mode causa:
1. L'istruzione `MOV [imm32], EAX` (opcode `A3`) viene decodificata come 9 byte in 64-bit
   (prefisso REX + indirizzo a 8 byte) → misalignment della catena di istruzioni.
2. Quando si arriva a `jmp 0x08:long_mode` (far jump con opcode `0xEA`): istruzione
   **invalida in 64-bit** → `#UD` → triple fault silenzioso (no IDT attivo).

Soluzione: entry separato `_uefi_start` (BITS 64) in `boot/boot.asm`, rilevato dal loader
tramite symbol table lookup. Non tocca il path Multiboot2.

### Nota: M1 — kernel-owned page tables

Le page tables correnti sono costruite dal bootloader UEFI e caricate in CR3 prima
dell'handoff. Il kernel le eredita. **M1 dovrà ricostruire page tables kernel-owned**
in `vmm_init()`, caricare il nuovo CR3, e dismettere la dipendenza dalle tabelle del
bootloader. Riferimento: `ROADMAP_SECoS.md` sezione M1.

## Stato Attuale del Boot
```
M0 COMPLETATO — UEFI boot chain verificato end-to-end
_uefi_start raggiunto, kernel_main eseguita
M1 non ancora iniziato
```